<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="MentorMe, Inc.">
    <title><%= @organization.name %> - <%= yield(:title) %></title>

    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </head>
  <body>
    <div class="container-fluid landing-layout">
      <div class="header">
        <%= link_to root_url do %>
          <%= image_tag @organization.logo.url, alt: 'Logo Image', id: 'logo' %>
        <% end %>
        <div class="header-links">
          <%= link_to "Login", sign_in_path, class: "btn btn-info" %><br><br>
          <%= link_to image_tag("icons/twitter_org.png", alt: "Facebook Us"), @organization.twitter_link, target: "_blank" %>
          <%= link_to image_tag("icons/facebook_org.png", alt: "Facebook Us"), @organization.fb_link, target: "_blank" %>
        </div>
      </div>
      <%= bootstrap_flash %>

      <% if content_for?(:form) %>
        <div class="hero-container">
          <%= image_tag @organization.hero_image.url, alt: 'Hero Image' %>
          <div class="float-form-container">
            <%= yield(:form) %>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
